<div class="col-sm-6 col-lg-4 mb-4 d-flex justify-content-center" style="position: absolute; left: 0; top: 0;">
    <div class="card bg-transparent border-0 rounded-0">
        <a href="{{ path('app_home_details', { 'id': article.id }) }}" style="text-decoration:none">
        <img src="{{ asset( 'build/images/' ~ article.image) }}" class="card-img-top border-0 rounded-0" alt="">
        <div class="card-body bg-transparent">
            <h5 class="card-title text-black titreCard">{{ article.title|upper }}</h5>
{#                {% for category in categories %}#}
                {{  dump(article.categories) }}
{#                {% endfor %}#}
{#                {{ dump(field.value) }}#}
                <p class="card-text"><small class="text-muted">{{  dump(article.categories.get("title")) }}</small></p>
{#            <p class="card-text">{{ article.description|u.truncate(100) }}</p>#}
{#            <a href="{{ path('app_home_details', { 'id': article.id }) }}" class="btn btn-dark">Détails</a>#}
        </div>
        </a>
    </div>
</div>


